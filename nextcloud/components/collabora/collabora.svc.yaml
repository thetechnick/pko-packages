apiVersion: v1
kind: Service
metadata:
  name: collabora
  labels:
    app: collabora
  annotations:
    package-operator.run/phase: deploy
spec:
  ports:
  - name: http
    port: 80
    targetPort: http
  selector:
    app: collabora
  type: ClusterIP